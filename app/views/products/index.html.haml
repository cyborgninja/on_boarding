= search_form_for @q do |f|
  = f.label :name_cont
  = f.search_field :name_cont
  = f.submit
%table
  %thead
    %tr
      %th
        = sort_link(@q, :name)
  %tbody
    - @products.each do |product|
      %tr
        %td
          = product.name
